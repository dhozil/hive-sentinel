{
  "name": "hive-sentinel-api-bundle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/bundle-api.mjs",
    "build:api": "node scripts/bundle-api.mjs"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}